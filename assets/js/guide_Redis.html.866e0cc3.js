"use strict";(self.webpackChunkwomeng_docs=self.webpackChunkwomeng_docs||[]).push([[274],{429(e,i,l){l.r(i),l.d(i,{comp:()=>r,data:()=>n});var a=l(641);const s={},r=(0,l(262).A)(s,[["render",function(e,i){return(0,a.uX)(),(0,a.CE)("div",null,[...i[0]||(i[0]=[(0,a.Fv)('<h1 id="redis" tabindex="-1"><a class="header-anchor" href="#redis"><span>Redis</span></a></h1><p><a href="https://java.doocs.org/high-concurrency/why-cache" target="_blank" rel="noopener noreferrer">doocs技术社区文档</a></p><h2 id="缓存雪崩" tabindex="-1"><a class="header-anchor" href="#缓存雪崩"><span>缓存雪崩</span></a></h2><p>缓存机器发生了意外，全盘宕机，此时每秒有非常多的请求打到数据库，数据库扛不住，就挂了。</p><p>缓存雪崩的事前事中事后的解决方案如下：</p><ul><li>事前：Redis 高可用，主从+哨兵，Redis cluster，避免全盘崩溃。</li><li>事中：本地 ehcache 缓存 + hystrix 限流&amp;降级，避免 MySQL 被打死。</li><li>事后：Redis 持久化，一旦重启，自动从磁盘上加载数据，快速恢复缓存数据。</li></ul><h2 id="缓存穿透" tabindex="-1"><a class="header-anchor" href="#缓存穿透"><span>缓存穿透</span></a></h2><p>故意请求缓存中不可能存在的数据，比如说数据库记录的id是从1开始的，结果我请求过来的ID全都是负数，那缓存中就不可能有啊，直接视缓存如无物，直接打给数据库，这种就很容易把数据库打死了。解决方法也很简单，每次系统中没查到，就写一个空值到缓存中去，这种下次来就直接找缓存了。如果它用的都是不同的负数ID，那设置空值就没有效果了，可以使用布隆过滤器。</p><ul><li>请求数据的 key 不存在于布隆过滤器中，可以确定数据就一定不会存在于数据库中，系统可以立即返回不存在。</li><li>请求数据的 key 存在于布隆过滤器中，则继续再向缓存中查询。</li></ul><h2 id="缓存击穿" tabindex="-1"><a class="header-anchor" href="#缓存击穿"><span>缓存击穿</span></a></h2><p>某个热点key，访问非常频繁，正处于集中式高并发访问的情况，那当这个key在失效的瞬间，大量请求直接打到数据库。</p><ul><li>如果这个数据基本上永远不会变的，可以尝试设置永不过期</li><li>用定时线程主动给它续期</li><li>用锁，给查询数据库的代码上锁，保证只有极少的请求能够直接访问数据库，其它请求稍后 查询缓存就可以了。</li></ul><h2 id="多个redis实例并发竞争" tabindex="-1"><a class="header-anchor" href="#多个redis实例并发竞争"><span>多个Redis实例并发竞争</span></a></h2><p>多个Redis实例同时并发写一个key</p><ul><li>分布式锁</li><li>用CAS乐观锁，在数据库保存一个时间戳字段，每次更新缓存的时候都对比一下数据库的时间戳是不是要比缓存的时间戳要新，如果是就可以写，否则就说明数据库的是旧数据，不能写缓存。</li></ul><h2 id="常用数据类型和使用场景" tabindex="-1"><a class="header-anchor" href="#常用数据类型和使用场景"><span>常用数据类型和使用场景</span></a></h2><p><a href="https://java.doocs.org/high-concurrency/redis-data-types" target="_blank" rel="noopener noreferrer">doocs 文档</a></p><h2 id="redis过期策略" tabindex="-1"><a class="header-anchor" href="#redis过期策略"><span>Redis过期策略</span></a></h2><p><a href="https://java.doocs.org/high-concurrency/redis-expiration-policies-and-lru" target="_blank" rel="noopener noreferrer">Redis 过期策略是：<strong>定期删除+惰性删除</strong>。</a></p><h2 id="持久化" tabindex="-1"><a class="header-anchor" href="#持久化"><span>持久化</span></a></h2><p><a href="https://java.doocs.org/high-concurrency/redis-persistence" target="_blank" rel="noopener noreferrer">Redis 的持久化有哪几种方式？不同的持久化机制都有什么优缺点？持久化机制具体底层是如何实现的？</a></p><p>Redis的持久化有两种方式，分别是</p><ul><li>RDB：周期性的持久化方式，也就是每隔一段时间就持久化一次。</li><li>AOF：对每条写入命令作为日志，以 <code>append-only</code> 追加的模式写入一个日志文件中，在 Redis 重启的时候，通过<strong>回放</strong> AOF 日志中的写入指令来重新构建整个数据。</li></ul><p>如果同时使用了两种持久化方式，那么在Redis重启的时候，会优先使用AOF来重新构建数据，因为AOF持久化的数据更加完整，一般都是每秒保存一次，也就是说如果最多只会丢失 1 秒钟的数据。</p><p>优缺点：</p><ul><li>RDB适合做冷备份，可以发送到一些远程存储系统上去，以防不测。</li><li>RDB恢复速度快，相对于AOF来说，RDB的恢复速度更快。</li><li>RDB数据丢失较多，它可能要隔很久才备份一次，而AOF是每秒钟保存一次。</li><li>AOF适合做灾难性的紧急恢复。</li><li>两种方式的性能也都不错。</li></ul><h2 id="如何保证缓存和数据库双写一致性" tabindex="-1"><a class="header-anchor" href="#如何保证缓存和数据库双写一致性"><span>如何保证缓存和数据库双写一致性</span></a></h2><p><a href="https://java.doocs.org/high-concurrency/redis-consistence" target="_blank" rel="noopener noreferrer">你只要用缓存，就可能会涉及到缓存与数据库双存储双写，你只要是双写，就一定会有数据一致性的问题</a></p><h2 id="分布式锁" tabindex="-1"><a class="header-anchor" href="#分布式锁"><span>分布式锁</span></a></h2><p>分布式锁是什么？为什么需要分布式锁？</p><blockquote><p>分布式锁是保证分布式集群环境下多线程并发安全性的一种手段。<br> 在没有分布式锁之前，锁只能保证单个应用内多线程的并发安全。在如今全民都分布式的情况下，普通的锁能够发挥的作用比较有限了。</p></blockquote><h3 id="分布式锁的特征" tabindex="-1"><a class="header-anchor" href="#分布式锁的特征"><span>分布式锁的特征</span></a></h3><ul><li>高性能</li><li>可重入</li><li>防死锁</li><li>互斥性</li></ul><h3 id="分布式常见应用场景" tabindex="-1"><a class="header-anchor" href="#分布式常见应用场景"><span>分布式常见应用场景</span></a></h3><ul><li>防止缓存击穿</li><li>任务调度</li><li>秒杀减库存防止超卖这种</li><li>保证接口幂等性</li></ul><h3 id="redis几种部署方式" tabindex="-1"><a class="header-anchor" href="#redis几种部署方式"><span>Redis几种部署方式</span></a></h3><ul><li><strong>单机</strong>：部署简单，但是极度害怕单点故障</li><li><strong>哨兵</strong>：一主多从，主从切换，比较稳。但是锁写到Master后，还没同步到Slave呢，Master挂了Slave选举成了Master，但是Slave里没有锁，其他线程再次能上锁了。不安全。</li><li><strong>集群</strong>：制作了slot分片，锁还是只写到master上，跟哨兵模式的问题相同。</li><li><strong>红锁</strong>：它是 Redis 实现分布式锁相对安全可靠的一种手段。红锁的核心思路是：搞几个独立的Master，比如5个，然后挨个加锁，只要超过一半以上（这里是<code>5 / 2 + 1 = 3</code>个）就代表加锁成功，然后释放锁的时候也逐台释放。这样的好处在于一台Master挂了的话，还有其他的，所以不耽误，看起来好像完美解决了上面的问题。但是它极其麻烦，而且对性能的开销也是其他锁的 N 倍，因为需要逐个加锁，需要多次与 Redis 通信。</li></ul><h3 id="redis-锁结构" tabindex="-1"><a class="header-anchor" href="#redis-锁结构"><span>Redis 锁结构</span></a></h3><p>锁其实是一个 redis 的 hash 结构，里面至少包含有：</p><ul><li>线程id（uuid）</li><li>过期时间</li><li>重入次数</li></ul><p>读写锁的话还有 锁类型（读锁还是写锁），且读锁可以有多个线程id，而且还要为每个线程id维护一个单独的过期时间。续期的时候不仅续期整个锁的过期时间，还要续期每个线程对应的过期时间。</p><h3 id="redis-加锁流程" tabindex="-1"><a class="header-anchor" href="#redis-加锁流程"><span>Redis 加锁流程</span></a></h3><ul><li><p>非公平锁：如果资源还没有锁，就上锁，添加过期时间。如果有锁，就判断是否是当前线程持有的锁，如果是就进行锁重入，重入次数加1，锁续期。如果不是当前线程持有的锁，那就是互斥了，就阻塞等待重试嘛。</p></li><li><p>公平锁：用zset和List按照加锁顺序进行排序，当持有锁的线程执行完之后，处在队列第一位的线程就能拿到锁。</p></li></ul><h2 id="redission" tabindex="-1"><a class="header-anchor" href="#redission"><span>Redission</span></a></h2><img src="/img/8.png"><h3 id="watchdog-看门狗机制" tabindex="-1"><a class="header-anchor" href="#watchdog-看门狗机制"><span>WatchDog 看门狗机制</span></a></h3><p>客户端第一次使用无参的lock()方法时，redis会另外启动一个线程检查锁过期时间，当时间超过三分之一的时候就会进行锁续期，续期使用的是lua脚本，保证了续期的原子性，而且这个专门用来续期的线程主要使用的是netty的时间轮容器，只需要一个线程就可以，对服务器性能消耗极小。另外一个需要注意的刚才也说到了，只有使用无参的lock()方法时才会开启看门狗续期，也就是说并不是所有lock方法都会使用看门狗续期，只有没自定义过期时间的才会开启，默认过期时间是30秒。</p>',47)])])}]]),n=JSON.parse('{"path":"/guide/Redis.html","title":"Redis","lang":"zh-CN","frontmatter":{},"headers":[{"level":2,"title":"缓存雪崩","slug":"缓存雪崩","link":"#缓存雪崩","children":[]},{"level":2,"title":"缓存穿透","slug":"缓存穿透","link":"#缓存穿透","children":[]},{"level":2,"title":"缓存击穿","slug":"缓存击穿","link":"#缓存击穿","children":[]},{"level":2,"title":"多个Redis实例并发竞争","slug":"多个redis实例并发竞争","link":"#多个redis实例并发竞争","children":[]},{"level":2,"title":"常用数据类型和使用场景","slug":"常用数据类型和使用场景","link":"#常用数据类型和使用场景","children":[]},{"level":2,"title":"Redis过期策略","slug":"redis过期策略","link":"#redis过期策略","children":[]},{"level":2,"title":"持久化","slug":"持久化","link":"#持久化","children":[]},{"level":2,"title":"如何保证缓存和数据库双写一致性","slug":"如何保证缓存和数据库双写一致性","link":"#如何保证缓存和数据库双写一致性","children":[]},{"level":2,"title":"分布式锁","slug":"分布式锁","link":"#分布式锁","children":[{"level":3,"title":"分布式锁的特征","slug":"分布式锁的特征","link":"#分布式锁的特征","children":[]},{"level":3,"title":"分布式常见应用场景","slug":"分布式常见应用场景","link":"#分布式常见应用场景","children":[]},{"level":3,"title":"Redis几种部署方式","slug":"redis几种部署方式","link":"#redis几种部署方式","children":[]},{"level":3,"title":"Redis 锁结构","slug":"redis-锁结构","link":"#redis-锁结构","children":[]},{"level":3,"title":"Redis 加锁流程","slug":"redis-加锁流程","link":"#redis-加锁流程","children":[]}]},{"level":2,"title":"Redission","slug":"redission","link":"#redission","children":[{"level":3,"title":"WatchDog 看门狗机制","slug":"watchdog-看门狗机制","link":"#watchdog-看门狗机制","children":[]}]}],"git":{"updatedTime":1767958643000,"contributors":[{"name":"womeng","username":"womeng","email":"donglongqin@qq.com","commits":2,"url":"https://github.com/womeng"}],"changelog":[{"hash":"e3b2754d8b38cde6e6de72d98068d736450b161a","time":1767958643000,"email":"donglongqin@qq.com","author":"womeng","message":"合并Redis的内容"},{"hash":"b8e7af09363e2aa76ceb6847db16cbf052e12d54","time":1767700977000,"email":"donglongqin@qq.com","author":"womeng","message":"提交文档"}]},"filePathRelative":"guide/Redis.md"}')}}]);